# 🛡️ VPN Checker Backend

Автоматическая система для поиска, проверки и сортировки VLESS/VMess/Trojan ключей.
Работает полностью на **GitHub Actions**, не требует вашего сервера.

## 🚀 Возможности
*   **Умная проверка:** Многопоточность (50 потоков) + кэширование результатов (History DB).
*   **Сортировка:** Автоматически раскладывает ключи по папкам:
    *   🇷🇺 `RU_Best` — ключи из белых списков для обхода блокировок в РФ.
    *   🇪🇺 `My_Euro` — отфильтрованные ключи только из Европы (без США/Китая).
*   **Авто-обновление:** Запускается каждые 4 часа, удаляет мертвые ключи.
*   **Подписки:** Генерирует готовые ссылки для вставки в приложения.

## 📂 Структура проекта
vpn-checker-backend/
├── checked/ # Сюда скрипт складывает результат
│ ├── RU_Best/ # Папка для РФ (белые списки)
│ │ └── ru_white.txt
│ ├── My_Euro/ # Папка для Европы (гео-фильтр)
│ │ └── my_euro.txt
│ ├── history.json # База данных проверок (кэш)
│ ├── geo_cache.json # База IP-адресов стран
│ └── subscriptions_list.txt # Готовые ссылки на подписки
├── main.py # Главный скрипт (Python)
├── requirements.txt # Зависимости
└── .github/workflows/ # Настройки авто-запуска
└── checker.yml

text

## 🛠 Как запустить у себя (Fork)

Хотите такой же чекер? Это просто!

1.  **Сделайте Fork** этого репозитория (кнопка сверху справа).
2.  Включите **Actions**:
    *   Перейдите во вкладку `Actions`.
    *   Нажмите кнопку `I understand my workflows, go ahead and enable them`.
3.  Дайте права на запись:
    *   `Settings` -> `Actions` -> `General` -> `Workflow permissions`.
    *   Выберите **Read and write permissions**.
    *   Нажмите `Save`.
4.  (Опционально) Отредактируйте `main.py`:
    *   Замените `URLS_RU` и `URLS_MY` на свои источники.
    *   В конце файла в переменной `GITHUB_USER_REPO` укажите свой ник (например: `vasya/vpn-checker`).

Всё! Теперь ваш чекер будет работать автоматически каждые 4 часа.

## 📊 Статус
![Auto Key Checker](https://github.com/kort0881/vpn-checker-backend/actions/workflows/checker.yml/badge.svg)

Автор: [kort0881](https://github.com/kort0881)
