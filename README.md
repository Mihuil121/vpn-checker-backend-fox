# 🛡 VPN Checker & Aggregator - Продвинутая система проверки VPN конфигураций

![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)
![License](https://img.shields.io/badge/License-MIT-green.svg)

**Профессиональная система для автоматического сбора, глубокой проверки и интеллектуальной сортировки VPN конфигураций с поддержкой множества протоколов.**

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Поддерживаемые протоколы](#-поддерживаемые-протоколы)
- [Как это работает](#-как-это-работает)
- [Установка и настройка](#-установка-и-настройка)
- [Использование](#-использование)
- [Структура проекта](#-структура-проекта)
- [Технические детали](#-технические-детали)
- [Локальный HTTP-сервер](#-локальный-http-сервер)

---

## 🚀 Возможности

### 🔍 Глубокая проверка работоспособности
- **Базовая проверка**: TCP/UDP соединение с измерением задержки (latency)
- **Глубокая проверка**: Реальная проверка работоспособности VPN-протокола (TLS handshake, стабильность соединения)
- **Автоматическое удаление**: Нерабочие серверы автоматически удаляются из списков
- **Blacklist система**: Провалившие проверку серверы добавляются в черный список

### ⭐ Интеллектуальная система рейтинга
- **5-звездочная система**: От 1★ (плохо) до 5★ (премиум)
- **Буквенные оценки**: A+ (премиум), A (отлично), B (хорошо), C (нормально), D (ниже среднего), F (плохо)
- **Взвешенная оценка качества**: Учитывает latency (40%), jitter (20%), bandwidth (20%), uptime (20%)
- **Визуальные индикаторы**: 🏆 (премиум), ⭐ (отлично), ✅ (хорошо), ⚡ (нормально), ⚠️ (плохо)

### 🌍 Географическая информация
- **Эмодзи флаги стран**: 🇷🇺 🇩🇪 🇳🇱 🇫🇷 и др. для визуального определения локации
- **Автоматическое определение страны**: По TLD домена, IP диапазонам, ключевым словам
- **Геосортировка**: Разделение на российские (RU_Best) и европейские (My_Euro) серверы
- **Автоматическое распределение**: Источники автоматически распределяются по тегам (RU/MY) на основе URL

### 🎯 Умная классификация по типу маршрутизации

#### **White List (Белый список)** - для обхода блокировок через маскировку
- **Только протоколы с SNI**: VLESS, VMess, Trojan с TLS
- **Проверка соответствия SNI**: SNI должен соответствовать доменам из официального белого списка
- **Исключение SS**: Shadowsocks не поддерживает SNI, поэтому не может быть в белом списке
- **Показывает SNI**: Каким сервером прикидывается (gosuslugi.ru, sberbank.ru, vk.com и т.д.)
- **Показывает CIDR**: Диапазоны IP если используются
- **Официальные домены**: Использует список разрешенных сервисов при отключении интернета

#### **Black List (Черный список)** - обход блокировок без маскировки
- **Протоколы с SNI, но SNI не в белом списке**: Обходят блокировки, но не подходят для белого списка
- **Протоколы без SNI**: Shadowsocks и другие протоколы без поддержки SNI
- **Полный доступ**: VPN, прокси, туннели

#### **Universal (Универсальные)** - все рабочие серверы
- **Объединяет**: И белые, и черные списки
- **Для общего использования**: Когда нужны все доступные серверы

### 📊 Расширенные метрики (опционально)
- **Jitter тест**: Измерение нестабильности соединения (стандартное отклонение задержки)
- **Bandwidth тест**: Измерение пропускной способности
- **Uptime статистика**: Процент успешных проверок за последние 20 проверок

### 🧠 Умные функции
- **Smart Chunking**: Автоматическая разбивка больших списков (>1000 ключей) на части
- **Кэширование результатов**: Проверенные серверы кэшируются на 12 часов
- **Дубликаты**: Автоматическое удаление дубликатов по хешу ключа
- **Фильтр мусора**: Удаление CN, IR, локальных IP, невалидных ключей
- **Аналитика**: Статистика проверок, история, blacklist
- **Автоматическое определение тега**: Источники автоматически распределяются на RU/MY по URL

---

## 🔌 Поддерживаемые протоколы

| Протокол | Поддержка | Особенности | Белый список |
|----------|-----------|-------------|--------------|
| **VLESS** | ✅ Полная | Парсинг параметров, SNI извлечение, TLS проверка | ✅ Да (если SNI в белом списке) |
| **VMess** | ✅ Полная | Base64 JSON декодирование, определение страны из `ps` | ✅ Да (если SNI в белом списке) |
| **Trojan** | ✅ Полная | TLS handshake проверка, SNI извлечение | ✅ Да (если SNI в белом списке) |
| **Shadowsocks (SS)** | ✅ Полная | Поддержка base64 и plain форматов | ❌ Нет (не поддерживает SNI) |
| **Hysteria2** | ✅ Полная | UDP проверка, специальная обработка | ⚠️ Зависит от поддержки SNI |

---

## 🔧 Как это работает

### Процесс проверки (пошагово)

```
1. ЗАГРУЗКА ИСТОЧНИКОВ
   ├─ Загрузка ключей из единого массива URLS
   ├─ Автоматическое определение тега (RU/MY) по URL
   ├─ Декодирование base64 (если нужно)
   ├─ Парсинг различных форматов (VLESS, VMess, SS, Trojan, Hysteria2)
   └─ Фильтрация мусора (CN, IR, локальные IP)

2. ДЕДУПЛИКАЦИЯ
   ├─ Создание хеша для каждого ключа
   └─ Удаление дубликатов

3. ПРОВЕРКА КЭША
   ├─ Проверка истории проверок
   ├─ Если проверен < 12 часов назад и alive = true → используем кэш
   └─ Иначе → добавляем в очередь на проверку

4. БАЗОВАЯ ПРОВЕРКА (для новых/устаревших)
   ├─ TCP соединение (для TCP протоколов)
   ├─ UDP пакет (для Hysteria2)
   ├─ Измерение latency (задержки)
   └─ Если не отвечает → пропускаем (не добавляем в blacklist сразу)

5. ГЛУБОКАЯ ПРОВЕРКА (только в полной проверке)
   ├─ TLS handshake (для TLS протоколов)
   ├─ Проверка стабильности соединения
   ├─ Проверка что сервер действительно работает
   └─ Если не проходит → blacklist

6. РАСШИРЕННЫЕ МЕТРИКИ (опционально)
   ├─ Jitter тест (5 попыток, стандартное отклонение)
   ├─ Bandwidth тест (отправка данных, измерение скорости)
   └─ Uptime расчет (из истории проверок)

7. КЛАССИФИКАЦИЯ
   ├─ Извлечение SNI из ключа
   ├─ Проверка соответствия SNI доменам из WHITE_LIST_DOMAINS
   ├─ Определение типа: white/black/universal
   │  ├─ white: только если протокол поддерживает SNI И SNI в белом списке
   │  ├─ black: если SNI не в белом списке или протокол без SNI (SS)
   │  └─ universal: все рабочие серверы
   ├─ Определение страны по TLD, IP, ключевым словам
   └─ Расчет рейтинга качества

8. СОРТИРОВКА И СОХРАНЕНИЕ
   ├─ Сортировка по latency (от быстрых к медленным)
   ├─ Разделение на категории: ru_white, ru_black, ru_universal, euro_white, euro_black, euro_universal
   ├─ Smart Chunking (разбивка на части если >1000)
   └─ Генерация subscriptions_list.txt
```

### Система классификации White/Black/Universal

**Логика классификации:**

1. **Определение протокола**: VLESS, VMess, Trojan, Shadowsocks, Hysteria2
2. **Извлечение SNI**: Из параметров ключа (sni, host, serverName)
3. **Проверка соответствия**: SNI сравнивается с доменами из `WHITE_LIST_DOMAINS`
4. **Классификация**:
   - **White**: Протокол поддерживает SNI (VLESS, VMess, Trojan) И SNI соответствует домену из белого списка
   - **Black**: Протокол поддерживает SNI, но SNI не в белом списке ИЛИ протокол не поддерживает SNI (SS)
   - **Universal**: Все рабочие серверы (и white, и black)

**Официальный белый список доменов включает:**
- Госуслуги: gosuslugi.ru, esia.gosuslugi.ru
- Банки: sberbank.ru, alfabank.ru, vtb.ru, tinkoff.ru, gazprombank.ru
- Операторы связи: beeline.ru, megafon.ru, mts.ru, rostelecom.ru
- Почта России: pochta.ru
- Маркетплейсы: ozon.ru, wildberries.ru, market.yandex.ru
- Соцсети: vk.com, ok.ru, mail.ru
- Яндекс сервисы: yandex.ru, music.yandex.ru, maps.yandex.ru, taxi.yandex.ru
- СМИ: kp.ru, ria.ru, rbc.ru, lenta.ru, tass.ru
- И другие разрешенные сервисы

### Система рейтинга качества

Рейтинг рассчитывается по формуле с весами:

```
Quality Score = (Latency × 40%) + (Jitter × 20%) + (Bandwidth × 20%) + (Uptime × 20%)
```

**Градация:**
- **90-100 баллов**: 🏆 5★ A+ (Премиум)
- **80-89 баллов**: ⭐ 5★ A (Отлично)
- **70-79 баллов**: ⭐ 4★ B+ (Очень хорошо)
- **60-69 баллов**: ✅ 4★ B (Хорошо)
- **50-59 баллов**: ✅ 3★ C+ (Нормально)
- **40-49 баллов**: ⚡ 3★ C (Приемлемо)
- **30-39 баллов**: ⚡ 2★ D (Ниже среднего)
- **0-29 баллов**: ⚠️ 1★ F (Плохо)

### Формат метки ключа

**Текущий формат (без YouTube статуса):**
```
🇳🇴NO_W_🏳️_SNI:telewebion.com_🏆5★A+_@vlesstrojan
│   │ │ │ │   │                    │        │
│   │ │ │ │   │                    │        └─ Канал
│   │ │ │ │   │                    └─ Рейтинг (иконка + звезды + оценка)
│   │ │ │ │   └─ SNI домен (только для white списков)
│   │ │ │ └─ Белый флаг (только для white списков)
│   │ │ └─ Тип: W (white), B (black), U (universal)
│   │ └─ Код страны с флагом
│   └─ Может включать: bandwidth, jitter, uptime
```

**Примеры меток:**

**Белый список:**
```
🇳🇴NO_W_🏳️_SNI:vk.com_🏆5★A+_@vlesstrojan
```
- Тип: W (white)
- SNI: vk.com (из официального белого списка)
- Рейтинг: 🏆 5★ A+ (премиум)

**Черный список:**
```
🇩🇪DE_B_⭐5★A_@vlesstrojan
```
- Тип: B (black)
- Нет SNI или SNI не в белом списке
- Рейтинг: ⭐ 5★ A (отлично)

**Универсальный:**
```
🇳🇱NL_U_✅4★B_@vlesstrojan
```
- Тип: U (universal)
- Стандартная маршрутизация
- Рейтинг: ✅ 4★ B (хорошо)

---

## 📦 Установка и настройка

### Требования

- Python 3.8 или выше
- Интернет соединение
- (Опционально) Для TUI режима: терминал с поддержкой curses

### Установка зависимостей

```bash
pip install -r requirements.txt
```

Зависимости:
- `requests` - для загрузки ключей из источников
- `websocket-client` - для WebSocket соединений (опционально)

### Настройка

Откройте `main.py` и настройте:

**1. Ваш канал/метка (строка ~65):**
```python
MY_CHANNEL: str = "@your_channel_name"
```

**2. Источники ключей (строки ~70-91):**
```python
URLS = [
    "https://raw.githubusercontent.com/...",
    # Добавьте свои источники
    # Автоматическое распределение по тегам (RU/MY) происходит по URL
]
```

**Функция автоматического определения тега:**
- **RU**: Если в URL есть маркеры: "russia", "rus", "mobile", "cable", "whitelist", "bypass", "reality"
- **MY**: Иначе (европейские/международные источники)

**3. GitHub репозиторий (строка ~2077):**
```python
GITHUB_REPO = "YOUR_USERNAME/YOUR_REPO"
```

**4. Настройки производительности (строка ~35):**
```python
TIMEOUT: int = 8              # Таймаут проверки (секунды)
THREADS: int = 50              # Количество потоков
CACHE_HOURS: int = 12          # Время кэширования
CHUNK_LIMIT: int = 1000        # Максимум ключей в файле
MAX_KEYS: int = 15000          # Максимум ключей для проверки
ENABLE_DEEP_TEST: bool = True  # Глубокая проверка по умолчанию
```

**5. Официальный белый список доменов (строки ~122-180):**
```python
WHITE_LIST_DOMAINS = {
    'gosuslugi.ru', 'sberbank.ru', 'vk.com',
    # Добавьте свои домены если нужно
}
```

---

## 💻 Использование

### TUI режим (графический интерфейс)

```bash
python main.py
```

**Меню:**
1. **Быстрая проверка** - только базовая проверка соединения (без глубокой проверки и метрик)
2. **Полная проверка (глубокая + метрики)** - полная проверка с глубокой валидацией
3. **Настройки** - изменение параметров проверки
   - `threads` - количество потоков (по умолчанию: 50)
   - `max_keys` - максимум ключей (по умолчанию: 15000)
   - `timeout` - таймаут в секундах (по умолчанию: 8)
   - `enable_bandwidth` - тест пропускной способности (по умолчанию: Выкл)
   - `enable_jitter` - тест нестабильности (по умолчанию: Выкл)
   - `enable_deep` - глубокая проверка (по умолчанию: Вкл)
   - `min_quality` - минимальное качество (по умолчанию: 30.0)
4. **Очистить кэш** - удаление истории, аналитики, blacklist
5. **Статистика** - просмотр статистики проверок
6. **Выход**

### CLI режим (командная строка)

```bash
# Полная проверка
python main.py --cli

# Быстрая проверка (без глубокой проверки)
python main.py --cli --fast

# С расширенными метриками
python main.py --cli --jitter --bandwidth

# С настройками
python main.py --cli --threads 100 --max-keys 20000 --timeout 10

# С минимальным качеством
python main.py --cli --min-quality 50.0
```

**Параметры CLI:**
- `--cli` - CLI режим (обязательно)
- `--fast` - быстрая проверка (без глубокой проверки)
- `--threads N` - количество потоков (по умолчанию: 50)
- `--max-keys N` - максимум ключей (по умолчанию: 15000)
- `--timeout N` - таймаут в секундах (по умолчанию: 8)
- `--jitter` - включить jitter тест
- `--bandwidth` - включить bandwidth тест
- `--min-quality N` - минимальный quality score (по умолчанию: 0.0)

---

## 📂 Структура проекта

```
vpn-checker-backend-fox/
├── main.py                    # Основной скрипт
├── server.py                  # Локальный HTTP-сервер
├── generate_links.py          # Генератор локальных ссылок
├── requirements.txt            # Зависимости
├── README.md                  # Документация
│
└── checked/                   # Результаты проверки
    ├── subscriptions_list.txt # Список всех подписок
    ├── history.json           # История проверок (кэш)
    ├── analytics.json         # Аналитика и статистика
    ├── blacklist.json         # Черный список нерабочих серверов
    │
    ├── RU_Best/               # Российские серверы
    │   ├── ru_white.txt       # Белый список (SNI из белого списка)
    │   ├── ru_black.txt       # Черный список (обход без маскировки)
    │   └── ru_universal.txt   # Универсальные (все рабочие)
    │
    └── My_Euro/               # Европейские серверы
        ├── euro_white.txt     # Белый список (SNI из белого списка)
        ├── euro_black.txt     # Черный список (обход без маскировки)
        └── euro_universal.txt # Универсальные (все рабочие)
```

### Формат файлов результатов

Каждый файл содержит список ключей в формате:
```
vless://uuid@host:port?params#метка_с_информацией
```

**Примеры меток:**

**Белый список:**
```
🇳🇴NO_W_🏳️_SNI:vk.com_🏆5★A+_@vlesstrojan
```
- Показывает SNI (каким сервером прикидывается)
- Флаг белого списка 🏳️
- Высокий рейтинг качества

**Черный список:**
```
🇩🇪DE_B_⭐5★A_@vlesstrojan
```
- Тип: B (black)
- Нет SNI или SNI не в белом списке
- Хороший рейтинг

**Универсальный:**
```
🇳🇱NL_U_✅4★B_@vlesstrojan
```
- Тип: U (universal)
- Стандартная маршрутизация

---

## 🔬 Технические детали

### Парсинг протоколов

**VLESS:**
```
vless://uuid@host:port?encryption=none&security=tls&sni=example.com&type=ws#label
```
- Извлекает: host, port, TLS статус, SNI из параметров (sni, host, serverName)
- Поддерживает: TCP, WebSocket, gRPC, HTTP/2

**VMess:**
```
vmess://base64_json
```
- Декодирует base64, парсит JSON
- Извлекает: add (host), port, tls, ps (описание), sni, host
- Поддерживает: TCP, WebSocket, HTTP/2

**Shadowsocks:**
```
ss://base64@host:port
ss://method:password@host:port
```
- Поддерживает оба формата
- **Не поддерживает SNI** → не может быть в белом списке

**Trojan:**
```
trojan://password@host:port?sni=example.com#label
```
- TLS протокол по умолчанию
- Извлекает SNI из параметров

**Hysteria2:**
```
hysteria2://password@host:port?insecure=1&sni=example.com
```
- UDP протокол, специальная проверка
- Поддержка SNI зависит от конфигурации

### Система классификации

**Алгоритм классификации:**

1. **Определение протокола**: `get_protocol_type(key)`
2. **Проверка поддержки SNI**:
   - Shadowsocks → автоматически `black` или `universal`
   - VLESS, VMess, Trojan → проверяем SNI
3. **Извлечение SNI**: `extract_sni_and_cidr(key)`
4. **Проверка соответствия**: `is_domain_in_white_list(sni)`
   - Проверяет прямое совпадение
   - Проверяет поддомены (m.vk.com → vk.com)
5. **Классификация**:
   - SNI в белом списке → `white`
   - SNI не в белом списке или нет SNI → `black`
   - Все рабочие → `universal`

**Функция проверки домена:**
```python
def is_domain_in_white_list(sni_domain: Optional[str]) -> bool:
    """
    Проверяет соответствие SNI домена доменам из белого списка.
    Учитывает поддомены (например, m.vk.com соответствует vk.com).
    """
```

### Система кэширования

- **Формат**: JSON файл с хешем ключа как ключом
- **Структура записи**:
```json
{
  "key_hash": {
    "alive": true,
    "latency": 100,
    "time": 1234567890,
    "country": "NO",
    "routing_type": "white",
    "deep_check": true
  }
}
```
- **Время жизни**: 12 часов (настраивается через `CACHE_HOURS`)
- **Автоочистка**: Старые записи (>3 дней) удаляются автоматически

### Blacklist система

- **Формат**: JSON файл
- **Структура**:
```json
{
  "hosts": ["example.com", "192.168.1.1"],
  "reasons": {
    "example.com": {
      "reason": "Connection timeout",
      "added": 1234567890,
      "failures": 3
    }
  }
}
```
- **Автоматическое добавление**: При провале глубокой проверки
- **Счетчик ошибок**: Отслеживание количества неудач

### Аналитика

- **Формат**: JSON файл
- **Структура**:
```json
{
  "key_hash": {
    "created": 1234567890,
    "checks": [
      {"time": 1234567890, "success": true, "latency": 100},
      {"time": 1234567900, "success": true, "latency": 95}
    ]
  }
}
```
- **Uptime расчет**: Процент успешных проверок из последних 20
- **История**: Хранится до 50 последних проверок на ключ

---

## 🌐 Локальный HTTP-сервер

Для раздачи файлов локально через HTTP.

### Запуск сервера

```bash
# Базовый запуск (порт 8000)
python server.py

# С указанием порта
python server.py -p 8080

# Только локальный доступ
python server.py --host localhost -p 8000

# Доступ из сети
python server.py --host 0.0.0.0 -p 8000
```

### Веб-интерфейс

Откройте в браузере: `http://localhost:8000`

**Возможности:**
- Просмотр всех доступных файлов
- Копирование ссылок одним кликом
- Группировка по категориям (RU_Best, My_Euro)
- CORS поддержка для доступа из браузера

### Использование в VPN клиентах

Добавьте ссылки в ваш VPN клиент:
```
http://localhost:8000/RU_Best/ru_white.txt
http://localhost:8000/My_Euro/euro_white.txt
```

### Генератор локальных ссылок

```bash
python generate_links.py
```

Создает файл `local_links.txt` со всеми локальными ссылками.

---

## 📊 Примеры результатов

### Белый список (White List)
```
vless://...@host:port?params#🇳🇴NO_W_🏳️_SNI:vk.com_🏆5★A+_@vlesstrojan
```
- Показывает SNI (каким сервером прикидывается - vk.com из белого списка)
- Флаг белого списка 🏳️
- Высокий рейтинг качества
- **Только протоколы с SNI**: VLESS, VMess, Trojan

### Черный список (Black List)
```
vless://...@host:port?params#🇩🇪DE_B_⭐5★A_@vlesstrojan
```
- Тип: B (black)
- SNI не в белом списке или протокол без SNI (SS)
- Хороший рейтинг

### Универсальный
```
vless://...@host:port?params#🇳🇱NL_U_✅4★B_@vlesstrojan
```
- Тип: U (universal)
- Стандартная маршрутизация
- Объединяет white и black списки

---

## ⚙️ Настройка фильтров

### Маркеры стран

**Европа (EURO_CODES):**
```python
EURO_CODES = {"NL", "DE", "FI", "GB", "FR", "SE", "PL", "CZ", "AT", "CH", "IT", "ES", "NO", "DK", "BE", "IE", "LU", "EE", "LV", "LT", "RO", "BG", "HR", "SI", "SK", "HU", "PT", "GR", "CY", "MT"}
```

**Плохие маркеры (BAD_MARKERS):**
```python
BAD_MARKERS = ["CN", "IR", "KR", "BR", "IN", "RELAY", "POOL", "🇨🇳", "🇮🇷", "🇰🇷", "TR", "SA", "AE"]
```

### Официальный белый список доменов

**WHITE_LIST_DOMAINS** включает:
- Госуслуги: gosuslugi.ru, esia.gosuslugi.ru
- Банки: sberbank.ru, alfabank.ru, vtb.ru, tinkoff.ru, gazprombank.ru
- Операторы связи: beeline.ru, megafon.ru, mts.ru, rostelecom.ru
- Почта России: pochta.ru
- Маркетплейсы: ozon.ru, wildberries.ru, market.yandex.ru
- Соцсети: vk.com, ok.ru, mail.ru
- Яндекс сервисы: yandex.ru, music.yandex.ru, maps.yandex.ru, taxi.yandex.ru
- СМИ: kp.ru, ria.ru, rbc.ru, lenta.ru, tass.ru
- И другие разрешенные сервисы

**Добавление своих доменов:**
```python
WHITE_LIST_DOMAINS = {
    # ... существующие домены ...
    'your-domain.com',  # Добавьте свой домен
}
```

### Классификация

**Белый список (WHITE_MARKERS) - только для информации:**
```python
WHITE_MARKERS = ["white", "whitelist", "bypass", "россия", "russia", "mobile", "cable", "госуслуг", "government", "banking", "bank", "RU", "МТС", "Beeline"]
```
*Примечание: Классификация теперь основана на проверке SNI, а не на маркерах*

**Черный список (BLACK_MARKERS):**
```python
BLACK_MARKERS = ["black", "blacklist", "full", "global", "universal", "all", "vpn", "proxy", "tunnel", "freedom"]
```

---

## 🐛 Решение проблем

### Проблема: Много нерабочих ключей

**Решение:**
- Включите глубокую проверку (`ENABLE_DEEP_TEST = True`)
- Увеличьте таймаут (`TIMEOUT = 10`)
- Проверьте источники ключей

### Проблема: Медленная проверка

**Решение:**
- Уменьшите количество потоков (`THREADS = 30`)
- Используйте быструю проверку (`--fast`)
- Уменьшите максимум ключей (`MAX_KEYS = 5000`)

### Проблема: Не определяются страны

**Решение:**
- Проверьте TLD доменов в `TLD_COUNTRY_MAP`
- Добавьте свои маркеры стран
- Проверьте формат ключей

### Проблема: Shadowsocks не попадает в белый список

**Решение:**
- Это нормально! Shadowsocks не поддерживает SNI, поэтому не может маскироваться под домены из белого списка
- Используйте VLESS, VMess или Trojan для белого списка

### Проблема: Серверы не классифицируются как white

**Решение:**
- Проверьте что протокол поддерживает SNI (VLESS, VMess, Trojan)
- Проверьте что SNI указан в параметрах ключа
- Проверьте что SNI соответствует домену из `WHITE_LIST_DOMAINS`
- Добавьте нужный домен в `WHITE_LIST_DOMAINS` если его там нет

---

## 📝 Лицензия

MIT License - используйте свободно.

---

## 🤝 Вклад

Приветствуются pull requests и issues!

---

## 📞 Поддержка

Если у вас есть вопросы или проблемы:
1. Проверьте документацию
2. Откройте issue на GitHub
3. Проверьте настройки в `main.py`

---

**Сделано с ❤️ для сообщества VPN пользователей**
